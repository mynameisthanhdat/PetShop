import React from 'react';
import {
    SafeAreaView,
    StyleSheet,
    ScrollView,
    View,
    Text,
    StatusBar,
} from 'react-native';

const History = ({ navigation }) => {
    return (
        <View>
            <Text>History page</Text>
        </View>
    )
}
export default History;